include pug/mixin
<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        link(rel="stylesheet", href="css/normalise.css")
        link(rel="stylesheet", href="css/style.css")
        title Test maket
    body
        // header
        .main-header__wrapper
            header.main-header
                .logo LIVE
                nav.nav
                    ul.nav__list
                        +nav_link("nav", "#", "news")
                        +nav_link("nav", "#", "sports")
                        +nav_link("nav", "#", "life")
                        +nav_link("nav", "#", "money")
                        +nav_link("nav", "#", "tech")
                        +nav_link("nav", "#", "travel")
                        +nav_link("nav", "#", "opinion")
                        li.navigation__item.weather
                            img(src="img/weather.png", alt="weather")
                            b 17
                ul.user-nav
                    li
                        button search
                    li
                        a.user-nav__subscribe(href="#") Subscribe
                    li
                        a(href="#") Sing In
        // /header

        // main
        main.main-container
            section.articals
                time(datatime="2019-03-18")
                ul
                - for( var i=0; i<10; i++)
                    li
                        Lorem ipsum dolor sit amet consectetur, adipisicing elit. Culpa fugit atque laborum esse maxime harum quam corrupti voluptas ipsa, odit, iste ipsum quas consequuntur suscipit! Incidunt officiis odit porro! Quaerat.

            section.slider
                img(src="img/ilon.jpg", alt="" height="600px" width="845px")

                ol.slider__pagination
                    - for( var i=1; i<=6; i++)
                        if (i==4)
                            li.slider__pagination--active slide #{i}
                        li slide #{i}
                
            

                    

